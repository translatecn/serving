apiVersion: v1
kind: Pod
metadata:
  name: title
  namespace: default
  labels:
    role: title
spec:
  containers:
    - name: title
      image: registry.cn-hangzhou.aliyuncs.com/acejilam/centos:7
      command:
        - "/bin/bash"
        - "-c"
        - "sleep 1d"
